{
  "devDependencies": {
    "@types/chrome": "^0.0.304",
    "esbuild": "^0.25.0",
    "typescript": "^5.7.3"
  },
  "scripts": {
    "watch": "esbuild plugin/**/*.ts --outdir=plugin/dist --bundle --watch --platform=browser"
  }
}
